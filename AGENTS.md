# VANI repository workflow

- After making edits, run the relevant checks, commit the task's changes, and push them to GitHub before handing the work back.
- Preserve unrelated user changes; do not silently include them in a commit.
- Do not force-push. If verification, committing, or pushing is blocked, report the blocker and any unpushed changes explicitly.
